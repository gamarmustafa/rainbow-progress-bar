<idea-plugin>
    <id>io.almabaw.progressbar</id>
    <name>Rainbow Progress Bar</name>
    <vendor email="qemerrr@gmail.com">qemerrr</vendor>

    <description><![CDATA[
        Replaces the boring stock progress bars with animated rainbow ones.
        Like the famous Nyan Cat progress bar &mdash; but without the cat,
        just pure scrolling rainbow goodness.
        <br/><br/>
        Works in the status bar, dialogs, the indexing bar, Gradle sync,
        and everywhere else the IDE shows progress.
        <br/><br/>
        The quirky marquee messages scrolling across the bar are fully
        editable in <b>Settings &gt; Appearance &amp; Behavior &gt;
        Rainbow Progress Bar</b>.
    ]]></description>

    <!-- Works in any IntelliJ-based IDE (Android Studio, IDEA, etc.) -->
    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <applicationConfigurable
            parentId="appearance"
            id="com.example.rainbow.settings"
            displayName="Rainbow Progress Bar"
            instance="com.example.rainbow.RainbowSettingsConfigurable"/>
    </extensions>

    <applicationListeners>
        <!-- Install the rainbow UI once the IDE frame exists. -->
        <listener
            class="com.example.rainbow.RainbowProgressBarActivator"
            topic="com.intellij.ide.AppLifecycleListener"/>
        <!-- A theme / Look-and-Feel switch resets ProgressBarUI to the platform
             default, so re-apply ours whenever the LaF changes. -->
        <listener
            class="com.example.rainbow.RainbowProgressBarActivator"
            topic="com.intellij.ide.ui.LafManagerListener"/>
    </applicationListeners>
</idea-plugin>
